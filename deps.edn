{:paths ["src"]
 ;; org.clojure/data.json matches com-gmail's JSON codec. The four kotoba-lang
 ;; libraries are consumed via :local/root sibling checkouts (they are not yet
 ;; published to Clojars or pinned to a git tag): com-gmail (the Gmail API v1
 ;; client ingest consumes), org-ietf-oauth2 (RFC 6749/7636 request/response
 ;; shaping for auth), mail (mail.inbound/mail.message address normalization),
 ;; langchain (langchain.db, the Datomic-API-compatible in-memory EAV store
 ;; that backs LangchainDbStore).
 :deps {
        ;; kotoba.bytes replaced java.util.Base64 here (2026-09-24). An explicit
        ;; floor: tools.deps takes the NEWEST sha it is shown.
        io.github.kotoba-lang/bytes {:git/sha "d5259f35ab7c4f5a66e63ca29ed4dcf7ba3f1ac5"}
        ;; kotoba.io replaced java.nio.file.Files here (2026-09-24). An explicit
        ;; floor: temp-dir temp-file read-all-bytes write-bytes write-string
        ;; delete-if-exists are in kotoba.io from this sha, and tools.deps takes
        ;; the NEWEST sha it is shown.
        io.github.kotoba-lang/io {:git/sha "1b60ca825c1e705bd195f857ca4549133653a9f3"}
        ;; kotoba.process replaced clojure.java.shell here (2026-09-24). An explicit
        ;; floor: kotoba.process/sh parses clojure.java.shell's options from db49ea9,
        ;; and tools.deps takes the NEWEST sha it is shown.
        io.github.kotoba-lang/process {:git/sha "db49ea936e53d0bc698be0db79bf96ad86b5f7d0"}
        ;; kotoba.lang.edn replaced clojure.edn here (2026-09-23). An explicit floor:
        ;; tools.deps takes the NEWEST sha it is shown, and kbb resolves git deps
        ;; only from nbb.edn, so every project file this repo has names it.
        io.github.kotoba-lang/edn {:git/sha "79dba32c8892058842c57783f06a2dfe4601aeb0"}
       ;; kotoba.test (clojure.test-compatible surface), not clojure.test --
        ;; scripts/migrate-clojure-test-to-kotoba-test.cljk. Explicit floor.
        io.github.kotoba-lang/test {:git/url "https://github.com/kotoba-lang/test.git"
                                    :git/sha "2ab4885f99fd25552d380d0f6b0b54c5a72eb7ae"}
        ;; kotoba.lang.text, not clojure.string. Pinned as an explicit floor:
        ;; tools.deps takes the NEWEST sha it is shown, so without a floor here
        ;; this repo silently rides whatever a sibling happens to name.
        io.github.kotoba-lang/text {:git/sha "73bdb13ae7a3d004b44bca08be03a3191157a38f"}
       io.github.kotoba-lang/json {:git/sha "15fd195ac41e6268e2005ccbf6cb9924e81e65e2"}
        io.github.kotoba-lang/com-gmail
        {:git/url "https://github.com/kotoba-lang/com-gmail.git"
                                     :git/sha "de7932b50979c3e2918ecb09f2b8543c93a37bcb"}
        io.github.kotoba-lang/org-ietf-oauth2
        {:git/url "https://github.com/kotoba-lang/org-ietf-oauth2.git"
                                     :git/sha "9dddbdcc58b02a72e28108ceb3bb4de5d10ba5c2"}
        io.github.kotoba-lang/mail
        {:git/url "https://github.com/kotoba-lang/mail.git"
                                     :git/sha "2a00bd8d63842dd2d1cf38d35bfb66ae377e98dc"}
        io.github.kotoba-lang/langchain
        {:git/url "https://github.com/kotoba-lang/langchain.git"
                                     :git/sha "51e7b61ec3d55e917d5dc77dac145197cb3340df"}}
 :aliases
 {:test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd6605cb6c0efc275e1df1736f6e90d4d73"}}
         :main-opts ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}
  ;; clj-kondo as a Clojars lib (no Homebrew/system install needed).
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts ["-m" "clj-kondo.main" "--lint" "src" "test"
                     "--fail-level" "error"]}}}